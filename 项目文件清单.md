# 慧速学 AI 伴学中心 · 项目文件清单

> 技术栈：Next.js（App Router） + React，单页 H5 落地页（家长成交型）。
> 本文梳理所有**页面级文件**（页面路由、API 路由、根布局、全局样式），不包含 `public/img` 静态图片与根目录文档（`.md`）。

| 文件名 | 功能描述 | 文件地址 |
| --- | --- | --- |
| `page.js` | 首页 H5 落地页（家长成交型单页）。包含 15 屏叙事（痛点、对比、真人伴学、学习报告、数学诊断、单词速记、中高考技巧、三模式伴学、校区、提分案例、免费体验、师资、环境、信任、预约），每屏内嵌高清图展示区；含 `IntersectionObserver` 滚动入场动效、手机号/校区校验的预约表单与提交状态管理，并对接 `/api/lead`。 | `app/page.js` |
| `layout.js` | 根布局（Root Layout）。定义 `<html lang="zh-CN">` 骨架、全站 SEO `metadata`（标题/关键词/描述）、OpenGraph 分享卡片、viewport 与主题色 `#2563eb`，并引入全局样式。 | `app/layout.js` |
| `route.js` | 预约线索提交接口（`POST /api/lead`）。校验体验类型/校区/11 位手机号，组装线索数据；配置了 Supabase 环境变量则写入 `leads` 表，否则降级为服务端日志模拟写入（不阻断用户）。 | `app/api/lead/route.js` |
| `globals.css` | 全局样式表。定义品牌配色（深蓝/科技蓝/成长橙/浅灰）、栅格与卡片（`.pain-card`/`.vs-card`/`.mode-card`/`.biz`/`.m3-card`/`.adv-card`/`.signup-card`/`.footbar`）、每屏 `.screen-img` 图片展示区，以及响应式与移动端适配。 | `app/globals.css` |

## 说明

- **页面路由**：`app/page.js` 对应线上根路径 `/`（单页，通过锚点 `id` 实现 15 屏内滑动切换）。
- **API 路由**：`app/api/lead/route.js` 对应 `/api/lead`，供预约表单异步提交。
- **非页面级文件**（未列入上表）：`public/img/` 下 15 张高清展示图、`public/` 其他静态资源、根目录 `package.json`/`next.config.mjs`/`README.md` 及营销文档 `.md` 等为配置或素材，不属于页面级文件。
