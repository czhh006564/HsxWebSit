这一套组合拳非常精准！有了**已认证的微信服务号**，加上 **Vercel + GitHub + 免费 Serverless 数据库** 的技术架构，不仅**全流程 0 托管成本**，还能实现顶级大厂级别的 CI/CD 自动化部署，且为未来用户注册、学习数据持久化留足了无限扩展空间。

作为“慧速学自动化”，我为您梳理一套针对这一现代化栈的**完整落地架构与服务号对接方案**：

---

## 1. 推荐技术选型 (全栈免费 Modern Stack)

这套架构能完美支持当前 H5 落地页/展示，以及未来“用户登录、学习记录、AI体检报告保存”等功能：

```text
 📱 微信服务号 (前端入口/菜单)
        │
        ▼
 🚀 Vercel 自动化托管 (Next.js / Nuxt 3)  ◄── GitHub 代码仓库 (自动构建)
        │
        ├── 🗄️ Supabase / Neon (免费 Serverless PostgreSQL 数据库)
        └── 🔑 Auth.js / Supabase Auth (用户注册/手机号/微信 OpenID)

```

| 模块 | 推荐方案 | 免费额度与优势 |
| --- | --- | --- |
| **全栈框架** | **Next.js (React)** 或 **Nuxt 3 (Vue)** | 支持 SSR 服务端渲染（微信内打开秒加载）+ API Routes（自带后端接口能力） |
| **代码管理与部署** | **GitHub + Vercel** | `git push` 代码即自动部署上线；Vercel Hobby 免费版每月包含 100GB 流量和无限 API 调用 |
| **云数据库** | **Supabase** (Postgres) 或 **Neon** | 免费额度 500MB 存储，自带图形化后台、实时 API 和内置用户鉴权体系 |
| **用户注册与鉴权** | **Supabase Auth** 或 **NextAuth.js** | 开箱即用支持 手机号+验证码 / 账号密码 / 微信 OpenID 绑定登录 |

---

## 2. 数据库表结构预设（未来注册与留资）

在 Supabase 中，您可以直接通过简单的 SQL 初始化两张核心表：

```sql
-- 1. 用户表 (未来注册账号用)
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone VARCHAR(20) UNIQUE,
  wechat_openid VARCHAR(64) UNIQUE,
  student_name VARCHAR(50),
  grade VARCHAR(20),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 2. 免费体验预约线索表 (当前即刻可用)
CREATE TABLE leads (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  lead_type VARCHAR(50), -- 学习力体检 / 单词速记 / 中考技巧
  subject VARCHAR(20),   -- 数学 / 英语 / 物理 / 化学
  campus VARCHAR(50),    -- 桥西 / 新华 / 长安 / 高新
  phone VARCHAR(20) NOT NULL,
  status VARCHAR(20) DEFAULT 'pending', -- pending / contacted / completed
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

```

---

## 3. 已认证服务号的对接优势与接入流程

已认证的服务号具备最高级别的 API 权限，对接流程如下：

### 第 1 步：挂载自定义菜单（直接跳转 Vercel 页面）

1. 登录 [微信公众平台](https://mp.weixin.qq.com)。
2. 进入 **内容与互动 $\rightarrow$ 自定义菜单**。
3. 创建菜单（例如：`20min免费体验` / `AI提分体系` / `个人中心`）。
4. 菜单内容选择 **跳转网页**，填入 Vercel 自动生成的链接（如 `https://huisuxue.vercel.app`）。

### 第 2 步：关注自动回复（首屏引流）

在 **自动回复 $\rightarrow$ 关注后自动回复** 中设置引导文案：

> “欢迎来到慧速学AI伴学中心！点击菜单栏 **[20min免费体验]**，即可现场测试孩子的知识缺陷或体验海马体单词速记！”

---

## 4. 💡 开发者避坑小贴士 (微信域名与 OAuth 授权)

在开发过程中，有以下两个关于微信生态的真实细节建议提前了解：

1. **自定义菜单跳转 vs. 微信静默授权**：
* **自定义菜单直接打开 Vercel 网址**：100% 正常使用，无任何限制。
* **微信网页授权（网页直接静默获取微信头像/OpenID）**：微信官方要求必须在服务号后台配置“网页授权域名”，且该域名**必须具备国内 ICP 备案**。
* **推荐解法**：在初期未买域名时，无需做复杂的 OAuth 静默登录，直接在 Vercel 网页上提供 **“手机号 + 验证码/密码注册”** 即可，这不仅免去了域名限制，还能直接拿到家长的有效联系方式。


2. **实时通知推送（通知老师跟进）**：
* 当家长在 Vercel 网页提交预约后，您的 Next.js/Nuxt 后端 API 可直接调用微信服务号的 **模板消息 API**，或者调用企业微信/Server酱机器人，直接在老师手机微信上弹出新线索提醒！



---

## 5. 项目搭建落地 4 步走

1. **新建 GitHub 仓库**：创建 `huisuxue-web` 项目并推送到 GitHub。
2. **关联 Vercel**：使用 GitHub 账号登录 Vercel，点击 “Import Project”，一键关联项目（之后每次 `git push` 自动更新网页）。
3. **初始化 Supabase 数据库**：在 Supabase 官网免费创建项目，复制 `NEXT_PUBLIC_SUPABASE_URL` 和 `KEY` 填入 Vercel 的环境变量（Environment Variables）。
4. **服务号配置**：将 Vercel 生成的链接配置到微信服务号自定义菜单。